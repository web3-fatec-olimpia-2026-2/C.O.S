# =====================================================================
# DEPENDÊNCIAS BASE DO PROJETO
# =====================================================================
# Este arquivo lista as bibliotecas que TODO grupo vai precisar.
# Cada grupo pode ADICIONAR mais conforme necessidade específica.
#
# Para instalar todas:
#   pip install -r requirements.txt
# =====================================================================

# ---------- Django e ecossistema ----------
Django>=5.0,<6.0
djangorestframework>=3.15,<4.0
django-cors-headers>=4.3
django-filter>=24.0

# ---------- Autenticação JWT ----------
djangorestframework-simplejwt>=5.3

# ---------- Documentação de API ----------
drf-spectacular>=0.27

# ---------- Configuração via .env ----------
django-environ>=0.11

# ---------- Banco de dados ----------
# SQLite vem embutido no Python (dev)
# PostgreSQL para produção (Railway/Render):
psycopg2-binary>=2.9

# ---------- MongoDB (a partir da Aula 7) ----------
pymongo>=4.6
# Alternativa com Django ORM: descomentar quando aplicável
# djongo>=1.3

# ---------- HTTP client para APIs externas ----------
httpx>=0.27
# Alternativa síncrona: requests>=2.31

# ---------- Servidor de produção ----------
gunicorn>=21.2
whitenoise>=6.6  # Servir arquivos estáticos

# ---------- Testes ----------
pytest>=8.0
pytest-django>=4.8
pytest-cov>=5.0
factory-boy>=3.3.3  # Fábricas para dados de teste
faker>=25.0      # Dados fake em português (locale pt_BR)

# ---------- Qualidade de código ----------
ruff>=0.5

# ---------- Utilitários ----------
python-decouple>=3.8  # Alternativa a django-environ
